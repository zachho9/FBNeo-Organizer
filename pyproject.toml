[project]
name = "fbneo-organizer"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pywebview>=6.2.1",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.20.0",
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
